app-id: io.github.alamahant.BinauralPlayer
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: BinauralPlayer
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
  - --filesystem=xdg-run/pipewire-0:ro
  - --filesystem=xdg-music:ro
  - --filesystem=xdg-documents/BinauralPlayer:create
modules:
  - name: Binauralplayer
    buildsystem: cmake-ninja
    config-opts:
      - -DFLATPAK_BUILD=ON
      - -DCMAKE_BUILD_TYPE=Release
    sources:
      - type: git
        url: https://github.com/alamahant/BinauralPlayer.git
        tag: v1.2.0
        commit: 5d256939bd8c72a1d77db747595722a41364b98e
